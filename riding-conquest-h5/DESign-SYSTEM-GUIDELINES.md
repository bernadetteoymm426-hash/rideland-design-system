# 骑迹大陆 - UI & VI 设计规范 v1.0

**项目名称**: 骑迹大陆 (RideLand)  
**文档版本**: v1.0  
**生成时间**: 2026-03-20  
**适用项目**: 骑迹大陆 H5 游戏化应用

---

## 1. 品牌识别系统 (VI)

### 1.1 品牌名称

| 类型 | 名称 | 说明 |
|------|------|------|
| **中文名称** | 骑迹大陆 | "骑迹" = 骑行轨迹 + 创造奇迹 |
| **英文名称** | RideLand | Ride (骑行) + Land (大陆) |
| **品牌 Slogan** | 骑行即征服——每一公里都在改变全国领土版图 | 核心体验承诺 |

### 1.2 品牌 Logo

**Logo 结构**:
```
┌─────────────────────────────┐
│    [自行车轮 + 地图轮廓]     │
│      骑迹大陆                │
│      RIDELAND               │
└─────────────────────────────┘
```

**Logo 使用规范**:
- 最小尺寸：宽度 ≥ 120px
- 安全边距：Logo 高度的 1/2
- 背景色：深色优先（#070E1B）

---

## 2. 色彩系统 (Color System)

### 2.1 主色调

| 颜色名称 | 色值 | 用途 | 示例 |
|---------|------|------|------|
| **深空黑** | `#070E1B` | 主背景色 | 地图底图、页面背景 |
| **科技蓝** | `#3B82F6` | 主色调、按钮、激活态 | 功能按钮、数据数字 |
| **翡翠绿** | `#10B981` | 积极数据、我方领土 | 里程数据、我方领土 |
| **胜利金** | `#F59E0B` | 成就、等级、荣誉 | 等级徽章、成就标识 |
| **热情红** | `#EF4444` | 重要提醒、敌方领土 | 敌方领土、警告提示 |

### 2.2 辅助色

| 颜色名称 | 色值 | 用途 |
|---------|------|------|
| **浅空灰** | `#F1F5F9` | 主文字色 |
| **中灰** | `#94A3B8` | 次要文字、标签 |
| **深灰** | `#64748B` | 辅助文字、时间戳 |
| **无主灰** | `#6B7280` | 无主领土标记 |

### 2.3 领土着色（半透明）

| 状态 | 色值 | 用途 |
|------|------|------|
| **我方领土** | `rgba(34, 197, 94, 0.45)` | 绿色半透明 |
| **敌方领土** | `rgba(239, 68, 68, 0.35)` | 红色半透明 |
| **争夺中** | `rgba(245, 158, 11, 0.4)` | 橙色半透明 |
| **无主之地** | `rgba(107, 114, 128, 0.2)` | 灰色半透明 |

### 2.4 渐变规范

**科技蓝渐变**:
```css
background: linear-gradient(180deg, #60A5FA 0%, #3B82F6 100%);
```

**金色荣誉渐变**:
```css
background: linear-gradient(180deg, #FBBF24 0%, #F59E0B 100%);
```

**绿色积极渐变**:
```css
background: linear-gradient(180deg, #34D399 0%, #10B981 100%);
```

---

## 3. 字体系统 (Typography)

### 3.1 字体家族

| 类型 | 字体栈 | 用途 |
|------|--------|------|
| **中文** | `-apple-system, BlinkMacSystemFont, "PingFang SC", "Microsoft YaHei", sans-serif` | 正文、标题 |
| **英文** | `Inter, SF Pro Display, -apple-system, sans-serif` | 英文、数字 |
| **等宽数字** | `SF Mono, Monaco, "Fira Mono", monospace` | 倒计时、数据 |

### 3.2 字号规范

| 级别 | 字号 | 字重 | 行高 | 用途 |
|------|------|------|------|------|
| **Display** | 32px | 700 | 1.2 | 页面大标题 |
| **H1** | 28px | 700 | 1.3 | 卡片标题 |
| **H2** | 24px | 600 | 1.4 | 章节标题 |
| **H3** | 20px | 600 | 1.4 | 小标题 |
| **Body** | 16px | 400 | 1.5 | 正文 |
| **Caption** | 12px | 400 | 1.5 | 辅助文字 |
| **Micro** | 10px | 400 | 1.4 | 标签、角标 |

### 3.3 字间距

| 类型 | 字间距 | 用途 |
|------|--------|------|
| **紧缩** | `0` | 正文 |
| **标准** | `0.5px` | 标题 |
| **宽松** | `1px` | 按钮文字 |
| **特宽** | `2-4px` | Logo 副标题 |

---

## 4. 布局系统 (Layout System)

### 4.1 横屏游戏化布局

**布局结构** (1920x1080 横屏):
```
┌──────────────────────────────────────────────┐
│  顶部信息条 (80px)                           │
│  Logo + 周循环状态条                          │
├──────────────────────────────────────────────┤
│                                              │
│          【中国地图 - 85% 屏幕】               │
│          高度约 900px                         │
│                                              │
├──────────────────────────────────────────────┤
│  底部功能栏 (100px)                          │
│  [战队] [作战] [战报] [排行]      👤 头像    │
└──────────────────────────────────────────────┘
```

**尺寸分布**:
- 顶部信息条：高度 80px
- 底部功能栏：高度 100px
- 地图区域：高度 ~900px (85%)

### 4.2 响应式断点

| 设备类型 | 宽度范围 | 布局调整 |
|---------|---------|---------|
| **桌面横屏** | ≥1920px | 完整布局 |
| **平板横屏** | 1024-1920px | 等比缩放 |
| **手机横屏** | 769-1024px | 功能栏简化 |
| **手机竖屏** | ≤768px | 竖屏适配（待设计） |

### 4.3 栅格系统

**基础栅格**: 8px 基准
- 间距：8px, 16px, 24px, 32px
- 圆角：8px, 12px, 16px
- 按钮高度：48px, 56px

---

## 5. 组件系统 (Component System)

### 5.1 按钮组件

**主按钮样式**:
```css
.btn-primary {
  background: rgba(26, 39, 64, 0.6);
  border: 1px solid rgba(59, 130, 246, 0.3);
  border-radius: 12px;
  padding: 12px 24px;
  color: #F1F5F9;
  font-weight: 600;
  letter-spacing: 1px;
}

.btn-primary:hover {
  background: rgba(59, 130, 246, 0.15);
  border-color: rgba(59, 130, 246, 0.5);
  transform: translateY(-2px);
  box-shadow: 0 8px 24px rgba(59, 130, 246, 0.3);
}
```

**5 种交互状态**:
1. **默认**: 深蓝背景 + 蓝色边框
2. **悬停**: 上浮 2px + 光效增强
3. **点击**: 下沉反馈
4. **禁用**: 灰色 + 无光效
5. **激活**: 更强蓝色光效 + 脉冲

### 5.2 功能按钮（底部栏）

**标准尺寸**:
- 宽度：100px
- 高度：80px
- 图标：32x32px
- 文字：13px

**视觉样式**:
- 背景：`rgba(26, 39, 64, 0.6)`
- 边框：`1px solid rgba(59, 130, 246, 0.3)`
- 圆角：12px
- 图标颜色：`#3B82F6`

### 5.3 浮层组件 (Overlay)

**标准浮层尺寸**:
- 宽度：800px (max), 90% (响应式)
- 高度：80vh (max)
- 圆角：16px
- 背景：`rgba(26, 39, 64, 0.95)`
- 边框：`1px solid rgba(59, 130, 246, 0.3)`
- 阴影：`0 16px 48px rgba(0, 0, 0, 0.5)`

**浮层结构**:
```
┌─────────────────────────────────┐
│ [× 关闭]                        │ ← 头部 (60px)
├─────────────────────────────────┤
│                                 │
│  [可滚动内容区]                 │
│  padding: 32px                  │
│                                 │
└─────────────────────────────────┘
```

**浮层动画**:
```css
@keyframes overlay-enter {
  from {
    opacity: 0;
    transform: scale(0.95) translateY(20px);
  }
  to {
    opacity: 1;
    transform: scale(1) translateY(0);
  }
}
```

### 5.4 周循环状态条

**尺寸**:
- 高度：48px
- 圆角：12px
- 背景：`rgba(26, 39, 64, 0.6)`
- 边框：`1px solid rgba(59, 130, 246, 0.3)`

**四阶段样式**:
```css
/* 对战期 */
.phase-combat {
  color: #3B82F6;
  border-color: #3B82F6;
}

/* 结算期 */
.phase-settlement {
  color: #F59E0B;
  border-color: #F59E0B;
}

/* 战报发布 */
.phase-report {
  color: #FBBF24;
  border-color: #FBBF24;
}

/* 宣战窗口 */
.phase-declare {
  color: #8B5CF6;
  border-color: #8B5CF6;
}
```

---

## 6. 地图系统 (Map System)

### 6.1 地图可视化

**ECharts 配置规范**:
```js
{
  geo: {
    map: 'china',
    roam: true,
    zoom: 1.2,
    center: [104.0, 36.0],
    itemStyle: {
      areaColor: '#0A1628',
      borderColor: '#3B82F6',
      borderWidth: 1,
      shadowColor: 'rgba(59, 130, 246, 0.5)',
      shadowBlur: 10
    }
  }
}
```

### 6.2 领土标记点

**标记点样式**:
- 尺寸：16px (直径)
- 边框：3px 白色
- 脉冲：2px 半透明边框
- 标签：12px 白色文字 + 黑色阴影

**颜色规范**:
- 我方：`#22C55E` (绿色)
- 敌方：`#EF4444` (红色)
- 无主：`#6B7280` (灰色)
- 家乡：`#F59E0B` (金色，特殊脉冲)

### 6.3 家乡高亮效果

**金色脉冲动画**:
```css
@keyframes hometown-pulse {
  0%, 100% {
    box-shadow: 0 0 20px rgba(245, 158, 11, 0.8);
    transform: scale(1);
  }
  50% {
    box-shadow: 0 0 40px rgba(245, 158, 11, 1);
    transform: scale(1.3);
  }
}
```

---

## 7. 情感化文案系统 (Emotional Copywriting)

### 7.1 家乡定位文案

**守护状态**:
```
标题：你的家乡 [北京] 正在被守护
副标题：[骑行者联盟] 已坚守 12 天
行动：骑一圈，为家乡加油
```

**被进攻状态**:
```
标题：你的家乡 [上海] 正在遭受进攻
副标题：[魔骑行] 的攻势还剩 2 天 14:32
行动：立即骑行，保卫家乡
```

**无主状态**:
```
标题：你的家乡 [杭州] 还是无主之地
副标题：成为第一个征服者，创造历史
行动：开始征服
```

### 7.2 贡献反馈文案

**1km**:
```
你的 1km 让战队攻城进度 +1%
每一公里都算数！
```

**10km**:
```
你的 10km 让战队攻城进度 +8%
离拿下浦东又近一步！
```

**50km**:
```
你的 50km 让战队攻城进度 +35%
个人贡献排名 TOP 10！
```

### 7.3 失败保护文案

**失去 1 个领土**:
```
虽然失去了浦东，但你的战队成功防守了 8 个领土
本周总骑行量增长了 40%！
```

**失去多个领土**:
```
本周形势严峻，但你的战队展现了顽强的防守精神
下周可以夺回来！
```

---

## 8. 动效系统 (Animation System)

### 8.1 基础动画

**缓动函数**:
```css
/* 标准缓动 */
--ease-standard: cubic-bezier(0.4, 0, 0.2, 1);

/* 弹性缓动 */
--ease-bounce: cubic-bezier(0.68, -0.55, 0.265, 1.55);

/* 快速缓动 */
--ease-fast: cubic-bezier(0.4, 0, 0.6, 1);
```

**持续时间**:
- 微交互：150ms
- 状态过渡：300ms
- 页面切换：500ms
- 大型动画：800ms

### 8.2 关键动画

**脉冲动画**:
```css
@keyframes pulse {
  0%, 100% {
    opacity: 1;
    transform: scale(1);
  }
  50% {
    opacity: 0.8;
    transform: scale(1.05);
  }
}
```

**上浮动画**:
```css
@keyframes float-up {
  from {
    opacity: 0;
    transform: translateY(20px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}
```

**缩放进入**:
```css
@keyframes scale-in {
  from {
    opacity: 0;
    transform: scale(0.95);
  }
  to {
    opacity: 1;
    transform: scale(1);
  }
}
```

---

## 9. 图标系统 (Icon System)

### 9.1 图标风格

**线条图标**:
- 尺寸：24x24px, 32x32px
- 描边：2px
- 端点：圆角
- 颜色：科技蓝 `#3B82F6`

**图标示例**:
```svg
<!-- 战队图标 -->
<svg viewBox="0 0 24 24" fill="none" stroke="#3B82F6" stroke-width="2">
  <path d="M17 21v-2a4 4 0 0 0-4-4H5a4 4 0 0 0-4 4v2"/>
  <circle cx="9" cy="7" r="4"/>
  <path d="M23 21v-2a4 4 0 0 0-3-3.87"/>
  <path d="M16 3.13a4 4 0 0 1 0 7.75"/>
</svg>
```

### 9.2 功能图标

| 功能 | 图标 | 用途 |
|------|------|------|
| 战队 | 多人轮廓 | 战队信息、成员管理 |
| 作战 | 交叉剑 | 宣战、进攻、驻防 |
| 战报 | 文档列表 | 周战报、战况回顾 |
| 排行 | 列表序号 | 战队/个人排行榜 |
| 个人 | 头像轮廓 | 个人中心、设置 |

---

## 10. 响应式设计 (Responsive Design)

### 10.1 断点规范

```css
/* 桌面横屏 */
@media (min-width: 1920px) {
  /* 完整布局 */
}

/* 平板横屏 */
@media (min-width: 1024px) and (max-width: 1920px) {
  /* 等比缩放 */
}

/* 手机横屏 */
@media (min-width: 769px) and (max-width: 1024px) {
  /* 功能栏简化 */
}

/* 手机竖屏 */
@media (max-width: 768px) {
  /* 竖屏适配 */
}
```

### 10.2 横屏优先原则

**设计决策**:
- 默认横屏布局
- 竖屏作为补充（简化视图）
- 地图页推荐横屏（可提示）

---

## 11. 无障碍设计 (Accessibility)

### 11.1 对比度标准

| 元素 | 最小对比度 | 目标对比度 |
|------|-----------|-----------|
| 正文文字 | 4.5:1 (AA) | 7:1 (AAA) |
| 大标题 | 3:1 (AA) | 4.5:1 (AAA) |
| 图标 | 3:1 (AA) | 4.5:1 (AAA) |

### 11.2 触控目标

**最小尺寸**: 44x44px  
**推荐尺寸**: 48x48px

---

## 12. 设计原则总结 (Design Principles)

### 12.1 UX 优先级框架

| 优先级 | 原则 | 含义 |
|--------|------|------|
| **P0** | 反馈优先 | 任何动作都必须有即时视觉反馈 |
| **P1** | 情感先于逻辑 | 先触动用户情感，再解释规则 |
| **P2** | 一目了然 | 核心信息扫一眼就懂 |
| **P3** | 智能默认 | 系统替用户做决定，用户只需确认 |
| **P4** | 地图即灵魂 | 地图是核心视觉和交互载体 |

### 12.2 情感化设计铁律

1. **永远正面表达** - 任何数据都用正向框架
2. **每骑必响** - 任何骑行后都有专属反馈
3. **紧迫不焦虑** - 营造期待感，不制造 FOMO
4. **集体叙事优先** - 个人数据放在集体故事语境中
5. **失败也有收获** - 即使输也展示积极面

---

## 13. 文件命名规范 (File Naming)

### 13.1 组件命名

```
components/
├── GameLayout.vue          # 游戏化布局
├── WeekStatusBar.vue       # 周循环状态条
├── GameBottomBar.vue       # 底部功能栏
├── PlayerAvatar.vue        # 玩家头像
├── ChinaMap.vue            # 中国地图
└── overlays/
    ├── TeamOverlay.vue     # 战队浮层
    ├── BattleOverlay.vue   # 作战浮层
    ├── ReportOverlay.vue   # 战报浮层
    ├── RankingOverlay.vue  # 排行浮层
    └── ProfileOverlay.vue  # 个人浮层
```

### 13.2 样式命名

**BEM 规范**:
```css
.block { }
.block__element { }
.block--modifier { }
```

**示例**:
```css
.game-button { }
.game-button__icon { }
.game-button--active { }
```

---

## 14. 版本历史 (Version History)

| 版本 | 日期 | 变更内容 | 作者 |
|------|------|---------|------|
| v1.0 | 2026-03-20 | 初始版本 - 横屏游戏化规范 | AI+Mark |

---

## 15. 使用指南 (Usage Guide)

### 15.1 给设计师

- 使用此规范确保视觉一致性
- 新增组件需符合色彩、字体、布局规范
- 浮层设计遵循标准尺寸和动画

### 15.2 给开发者

- 组件实现需符合本规范
- CSS 变量使用规范中定义的颜色
- 动画使用规范中定义的缓动函数

### 15.3 给 AI (Stitch)

**使用此规范生成 UI 时**:
1. 严格遵循色彩系统
2. 使用规范中定义的尺寸和间距
3. 浮层遵循标准布局
4. 动画使用规范中定义的 keyframes
5. 文案遵循情感化规范

---

**文档结束**

**下次更新**: 2026-03-27 (Week 1 结束)  
**维护者**: 骑迹大陆设计团队
